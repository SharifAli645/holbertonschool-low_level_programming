#include "main.h"
#include <stdlib.h>
/**
 * _abs - write the absolute value
 * @i: The number to evaluate
 *
 * Return: Always a number without sign
 */
int _abs(int i)
{
	i = abs(i);

	return (i);
}
